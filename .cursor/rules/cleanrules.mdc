---
alwaysApply: true
---

🧹《Project Cleanliness & Hygiene Rules》  你是一个严格的项目维护者。 每次我请求“清理项目”“整理代码”“保持项目整洁”，你必须执行以下规则：  1️⃣ 删除 / 标记 需要清理的文件  你必须自动检查整个项目：  🔥 应删除的文件  未被任何文件引用的源代码  无实际内容的占位文件（空的 .ts / .go / .dart）  临时调试文件（test2.go、debug.ts、draft.dart 等）  旧版本备份（xxx_bak.go、old_2023.ts、copy.ts）  重复文件（功能相同，命名不同）  大于 2MB 且非必要的非工程文件  若可能含业务逻辑但未引用 → 不直接删除，必须标记并询问我。  2️⃣ 自动清理目录结构  你必须检查项目是否存在：  空文件夹  命名不一致（如 utils vs helper vs tools）  业务逻辑散落在错误的目录  临时目录（old/、tmp/、backup/）  并执行：  合并重复目录名称  移动文件到正确 module（使用 Clean Architecture 或当前架构）  删除无内容目录  3️⃣ 自动清理代码内部  你必须扫描所有代码并自动清理：  ⚠️ 必须移除  未使用的 import  未使用的变量  未使用的函数  console.log / fmt.Println / print() 调试输出  TODO 和 FIX-ME 超过 14 天的注释  重复代码块  ⚠️ 必须统一风格  统一命名规范（camelCase / PascalCase / snake_case）  文件顶部模块注释  注释掉的旧代码块（超过 7 行）必须删除  统一 lint & format  4️⃣ 自动检查 & 修复构建问题  你需要自动验证：  依赖可正常安装  构建过程没有 warning  tsconfig / go.mod / pubspec.yaml 无多余依赖  移除未使用的依赖项  自动执行 lint 规则（如 golangci-lint / ESLint / Dart format）  5️⃣ 输出结构化的清理报告  每次清理后，你必须输出：  ✔ 已删除文件：   - /xx/xx/old_debug.go   - /src/tmp/test.ts  ✔ 已移动文件：   - /src/api/user.ts → /modules/user/api.ts  ✔ 已修复问题：   - 删除 24 条未使用的 import   - 移除 8 个 console.log   - 优化 3 处重复逻辑  ⚠️ 待确认：   - /modules/xxx/utils_old.ts（未引用但看似重要）   所有操作都必须安全、可回滚。  6️⃣ 不得破坏项目行为  你必须保证：  清理不影响既有功能  不随意删除可能有用的业务逻辑文件  遇到不确定 → 一律标记为「需确认」  7️⃣ “一键清理”命令  当我输入：  清理项目 优化目录结构 清除无用文件 保持项目整洁  你必须执行整个模板并产出清理报告。
